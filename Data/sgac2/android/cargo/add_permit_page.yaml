# SGAC2.0 Add Permit (Android) — the permit flow now lives INSIDE the web Convoy Submission
# flow (see cargo_convoy_page.yaml), not the native page SGAC1.0 had (PermitsAddPermit /
# Enter Permit No Manually / Scan Permit / SAVE PERMIT / CaptchaModal). Not walked in 2.0:
# it sits behind the Convoy webview and needs the staging tunnel + a full convoy submission
# to reach. All sgac1 native keys removed pending a web-flow walk (see parity allowlist).
# TODO(cargo-web): walk the permit step inside the Convoy webview and capture its DOM locators.
CARGO-PERMIT-PENDING-WEB-WALK: //android.widget.TextView[@text="Convoy Submission"]

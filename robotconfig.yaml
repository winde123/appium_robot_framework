### Fork-agnostic device/Appium config. Per-fork app values live under FORKS: below.
APPIUM_SERVER_URL: http://127.0.0.1:4723

### Android config
ANDROID_AUTOMATION_NAME: UIAutomator2
ANDROID_PLATFORM_NAME: Android
ANDROID_PLATFORM_VERSION: "%{ANDROID_PLATFORM_VERSION=16}"
ANDROID_DEVICE_NAME: RFCN90WD11Y
ANDROID_EMULATOR_NAME: emulator-5554

### iOS config
IOS_AUTOMATION_NAME: XCUITest
IOS_PLATFORM_NAME: iOS
IOS_PLATFORM_VERSION: "18.6.2"
IOS_DEVICE_NAME: Edwin's iPad
IOS_DEVICE_UDID: 00008122-000A08312186801C
IOS_XCODE_ORGID: W6PMZD7K72

### Per-fork app identity and binaries (docs/refactor/fork-conventions.md §4).
### Consumed ONLY by Resources/fork_config.py — nothing else reads FORKS directly.
### There is NO ios_binary key: iOS ships via TestFlight; sessions launch the
### installed build by bundle ID.
FORKS:
  sgac1:
    android_package: sg.gov.ica.mobile.app
    android_activity: sg.gov.ica.mobile.app.MainActivity
    ios_bundle_id: sg.gov.ica.mobile.app
    android_binary: icaApp/sgac1/app.apk
  sgac2:
    android_package: sg.gov.ica.mobile.app        # verified by T00 (same as sgac1)
    android_activity: sg.gov.ica.mobile.app.MainActivity
    ios_bundle_id: TODO(T00/T30)                  # placeholder — real value lands in T30
    android_binary: icaApp/sgac2/app.apk
